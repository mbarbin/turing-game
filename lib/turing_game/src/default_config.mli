(*_ This file does some side effect via the [Config] module but doesn't export
  anything. We make sure it is linked with the library thanks to the [linkall]
  option configured in the [dune] file in this directory. *)
